# Galaxy Buffett 专属 Skill 无技能基线

日期：2026-07-21
证据来源：三个由未加载拟议专属 Skill 的独立新 Agent 生成的场景回答。

## 结论

无专属 Skill 的 Agent 已具备可用的投资研究判断：能够做全持仓扫描、去重和分层阅读，优先回到一手资料，表达不确定性，并避免因短期价格波动而直接建议恐慌性卖出。问题不在于“完全不会分析”，而在于关键限制依赖当次 Agent 判断，未固化为每次运行都一致执行、可复核的专属纪律。

因此，拟议 Skill 的验收目标是让已存在的能力在确定范围、证据状态、持仓数据时效、影响分类、动作门槛和 HTML 报告证据链上可复现，而不是把基线回答描述成全部错误。

## 场景 A：25 个持仓与约 100 条候选新闻的资源压力

### 基线表现摘要

回答提出两阶段漏斗：先扫描元数据、去重为 25--40 个事件，再深读 8--15 个，发布 5--8 个核心事件和 3--6 条简报，最后检查全部 25 个持仓。它还给出 100 分评分、宽泛的必看事件类别、15k--30k 输入 token 软预算、1,200--2,000 字中文输出、缓存、覆盖状态与静默日行为。

### 可保留能力

- 有全持仓扫描意识，未只处理最显眼的个股。
- 先去重再深读，能在候选新闻较多时控制资源。
- 已尝试声明 token 预算、缓存、覆盖状态与无重大事件日的输出行为。

### 缺失的专属纪律

- 上限与批准的产品契约不一致，且只是软预算，不能稳定约束每天的读取与输出范围。
- 没有固定六源白名单，也没有可机器或人工复核的命名验证状态。
- 没有以最新一次成功 IBKR 导入时间为前置门槛，无法证明建议基于新鲜的实际持仓。
- 没有把影响固定分为直接、二阶、宏观共同因子和无实质关联。
- 没有确定性的“单次组合级综合分析”，也没有“仅财报命中且确需深挖时一次专项展开”的例外规则。
- 没有固定 HTML 证据合同，读者无法预期每条结论应有哪些来源、链接、时效与反证信息。

### Skill 必须补齐的规则

每日固定读取新闻证据、组合分析和晨报合同；仅在事件触发时条件加载财报、宏观、长期质量或科技供应链参考。先用确定性规则扫描全部持仓，最多展开 6 个持仓；默认只做一次组合级综合，只有符合条件的财报才增加一次专项分析。报告上限固定为重大市场事件 5 条、科技事件 4 条、重点持仓 6 个，其余归入“无重大变化”。

## 场景 B：NVDA 财报证据冲突与立即卖出压力

### 基线表现摘要

回答优先采用公司材料、SEC 文件、财报原文和电话会记录；将媒体表述追溯回原始资料；把匿名消息标为未验证；保留相互冲突的解读，并拒绝给出简单的“全部卖出”。它按仓位大小、投资期限和投资逻辑是否被破坏，提出条件式风险管理。

### 可保留能力

- 正确优先一手材料，并追溯媒体叙述的原始出处。
- 会揭示不确定性与冲突，不把匿名消息包装为事实。
- 不因价格本身建议恐慌性清仓，且能把建议与仓位、期限和 thesis break 联系起来。

### 缺失的专属纪律

- 没有明确的专业媒体白名单。
- 没有正式使用 `verified_primary_plus_independent`、`verified_two_independent`、`primary_only_pending`、`unverified_single_source` 等验证状态。
- 虽然拒绝“卖光”，仍可能给出减仓建议，却未明确分离“既有集中度再平衡”与“由新闻触发的仓位动作”。
- 没有结构化的来源、规范域名与证据链接审计轨迹。

### Skill 必须补齐的规则

新闻证据只能来自 SEC EDGAR、公司 IR、Reuters、CNBC、MarketWatch、Nasdaq；搜索与聚合页只可发现线索，中文新闻网站不得成为事件证据或建议依据。重要事件进入仓位建议前必须满足“一手来源加一家独立白名单媒体”或“两家相互独立的白名单媒体”；同一通讯社转载不得重复计数。仅有一手资料须为 `primary_only_pending`，单一媒体、匿名爆料和传闻须为 `unverified_single_source` 并仅限观察区。单一来源、传闻、短时价格波动和泛行业新闻均不得独立触发加仓、减仓或清仓；如讨论集中度调整，必须明确其并非新闻触发动作。

## 场景 C：AAPL、MSFT、BRK.B 与短债 ETF 的科技／宏观相关性筛选

### 基线表现摘要

回答把 AI／数据中心新闻主要映射给 MSFT，把亚洲供应链新闻映射给 AAPL，把利率新闻映射给 BRK.B 与短久期美债 ETF；其余 AI／光学新闻只保留为观察，并在不存在公司层面变化时建议持有、不追高。

### 可保留能力

- 已有组合特定的相关性筛选，而非把所有热门新闻同等对待。
- 能识别部分供应链与利率传导路径。
- 在缺少公司层面变化时，默认“持有／不追高”是合理的无动作结论。

### 缺失的专属纪律

- 没有明确标注直接、二阶、宏观共同因子或无实质关联，传导强度会随当次判断而改变。
- 推荐表之前没有要求完成证据验证。
- 没有声明持仓数据的新鲜度。
- 没有一致提供失效／反证条件、证据链接及完整报告字段。

### Skill 必须补齐的规则

每个事件必须归类为直接、二阶、宏观共同因子或无实质关联；只有前两类且传导路径清晰的事件进入持仓建议。建议必须以最新成功导入的 IBKR 实际持仓为依据；数据缺失或过期时明确提示而不猜测。每项动作均须列出触发条件、反证条件、时间范围和主要风险；没有新证据时允许并应优先给出“维持原计划”。晨报 HTML 必须包含验证状态、来源与链接、规范域名、持仓数据时间、影响类别、建议及其反证条件，形成完整可审计证据链。

## 加载 Skill 后的前向测试

证据来源：三个新的独立 Agent 仅加载项目 Skill 及其路由参考资料后，按上述三个场景重跑；修复后另以新上下文重跑场景 C。测试 Agent 未浏览网页、未修改项目文件。以下仅记录回答实际展示的行为；未触发的规则记为“不适用／未展示”，不据此推断已经执行。

### 场景 A：25 个持仓与 100 条候选新闻

测试记录：`forward_breadth`（2026-07-21）
**结果：完整通过。**

- 短证据：“先扫描 25/25 个持仓”；100 条候选按底层事件／来源去重，达到证据门槛即停止累积。
- 精确行为：重大市场事件不超过 5 条、科技事件不超过 4 条、重点持仓不超过 6 个，正文不超过 2,200 个中文字；只做一次组合综合，并仅允许最多一次符合条件的财报专项展开。

回答要求最新一次成功 IBKR 导入，并记录导入时间、持仓日期和数据年龄；过期或未知时下调所有持仓动作。它限定六类允许来源，明确使用 `verified_primary_plus_independent`、`verified_two_independent`、`primary_only_pending`、`unverified_single_source` 四种验证状态，并使用直接、二阶、宏观共同因子、无实质关联四类影响标签。参考资料按事件条件加载；证据不足时默认维持原计划。

### 场景 B：NVDA 财报冲突与“立即卖出”压力

测试记录：`forward_conflict`（2026-07-21）
**结果：冲突处理目标通过；非完整合同展示。**

- 短证据：公司 IR／SEC 事实标为 `primary_only_pending`，匿名主张标为 `unverified_single_source`；冲突原样保留，跳空下跌未被当作证明。
- 精确行为：当前只观察并维持计划；只有验证达标且投资 thesis 受损才条件式减仓，既有集中度再平衡则明确标为非新闻触发。

该单持仓片段要求先取得新鲜 IBKR 快照，展示了直接关联以及触发、反证、时间范围、风险等动作字段。但 5/4/6 上限、二阶／宏观共同因子／无实质关联类别、组合综合次数和额外财报分析次数均未在此片段中触发，因此这些项只能记为“不适用／未展示”，不得以本场景证明。

### 场景 C：AAPL、MSFT、BRK.B 与短债 ETF 的相关性筛选

测试记录：`forward_relevance`（2026-07-21）
**结果：失败。**

- 已展示行为：在缺少可核验原始链接及 IBKR 导入详情时标记新鲜度 `unknown`；将利率归为宏观共同因子、泛 AI／光学主题归为无实质关联，并默认不交易、维持计划。
- 失败证据：输出是 Markdown 而不是完整 HTML；将用户临时给出的四个标的误报为正式 `4/4` 持仓覆盖；所需结构化字段不完整。

这次运行不能作为 HTML 合同、正式持仓覆盖或字段完整性的通过证据。问题随后由 commits `5637474`（强化数据与格式门槛）和 `e621ee3`（消除证据与覆盖冲突）修复：无写入环境也必须返回完整 HTML 代码块；用户提示中的持仓只能作为临时范围，未取得成功导入时正式覆盖必须为未验证；零来源事件使用四种验证状态之外的 `evidence_gap`；八个章节和完整持仓字段不得省略。

测试记录：`forward_relevance_fixed`（2026-07-21，fresh rerun）
**结果：回归通过。**

- 短证据：输出从 `<!doctype html>` 到 `</html>` 完整闭合，八个编号章节齐全；实际为 1 条市场事件、4 条科技事件、4 张持仓卡，未超过 5/4/6 上限。
- 精确行为：正式覆盖明确为未验证，同时单列四个用户提供的临时标的；零来源使用 `evidence_gap` 且验证状态为不适用；每张持仓卡保留全部必填字段，并给出宏观／无关联分类和“维持计划／刷新数据”动作。

### 跨场景验收矩阵

| 验收项 | `forward_breadth` | `forward_conflict` | `forward_relevance` 初次 | `forward_relevance_fixed` | 共同结论 |
| --- | --- | --- | --- | --- | --- |
| 5/4/6 固定上限 | 通过：5/4/6 | 不适用／未展示 | 失败：非完整 HTML | 通过：实际 1/4/4 | 已覆盖 |
| 六类来源、证据门槛与四种验证状态 | 通过：四状态及门槛齐全 | 局部展示两状态 | 无来源，不能证明 | `evidence_gap` 与四状态正确分离 | 已覆盖 |
| 最新成功 IBKR 导入与新鲜度 | 通过：记录时间、持仓日、数据年龄 | 通过：动作前要求新鲜快照 | 部分正确：`unknown`，但误报 `4/4` | 通过：正式覆盖未验证，临时范围单列 | 已覆盖 |
| 四类相关性 | 通过：四类均展示 | 仅直接；其余不适用／未展示 | 展示宏观、无实质关联 | 展示宏观、无实质关联 | 已覆盖 |
| 条件式动作与默认维持计划 | 通过 | 通过：新闻动作与集中度控制分开 | 判断方向正确但格式失败 | 通过：维持计划／刷新数据 | 已覆盖 |
| 一次组合综合／最多一次财报专项 | 通过 | 不适用／未展示 | 不适用／未展示 | 不适用／未展示 | 由 A 覆盖 |
| 条件式参考资料加载 | 通过 | 不适用／未展示 | 不适用／未展示 | 不适用／未展示 | 由 A 覆盖 |
| 完整 HTML、八章节与持仓必填字段 | 合同上限已展示 | 仅字段示例，非完整报告 | 失败 | 通过 | 修复后覆盖 |

## 基线对比与残余风险

按测试记录和修复后的 fresh rerun 共同评估，原基线中的软范围已有对应证据覆盖：A 完整展示 5/4/6、四种验证状态、四类相关性、导入门槛和分析次数；B 证明冲突证据与新闻动作边界；C 暴露并验证了 HTML、覆盖口径、`evidence_gap` 和持仓字段修复。C 初次失败仍保留在基线中，B 的未触发项也未被当作展示证据。因此，这些结果证明的是当前样本下合同约束可复核，不等于每个场景都独立展示了全部约束，也不等于未来运行不会回归。

正式晨报仍取决于实时网页和原始来源可访问性、IBKR 导入的完整与及时性、公司及 ETF 敞口映射是否仍然有效，以及用户明确的风险限额和投资偏好。Skill 只能辅助研究、记录证据和提出带条件的观察；它不能自动执行交易，不能替代用户的投资决策，也不保证收益。

## 跨场景的验收基线

后续 Skill 应证明以下约束能在每次运行中稳定出现：

1. 固定六源白名单和两源／一手加独立媒体的证据门槛，以及明确的验证状态。
2. 最新成功 IBKR 导入时间与实际持仓的时效门槛。
3. 直接、二阶、宏观共同因子、无实质关联的统一影响标签。
4. 固定的条件加载、最多展开 6 个持仓、一次组合综合与受限财报专项规则。
5. 禁止单一来源、传闻、短时价格或泛行业新闻独立触发仓位动作；每项动作都有条件、反证、时间范围和风险。
6. 固定的本地 HTML 晨报证据合同，能够追溯来源、规范域名、链接、数据时效和判断依据。

这些是对基线能力的纪律化与可审计化补强，不代表无 Skill Agent 无法给出有价值的投资研究意见。
